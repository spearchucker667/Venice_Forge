# Test gaps

## Closed during remediation

1. Production server static-root behavior now has a real bundled/spawned integration regression test and a live HTTP probe.
2. The existing CSP invariant is green and packaged smoke proves both its positive path and deliberate negative control.
3. Primary chat IME composition has focused coverage; the centralized predicate is applied to all reviewed Enter-triggered text actions.
4. Document Agent's working-group selector has a localized named-role regression assertion.
5. Workspace approval ownership has session-family and IPC regression coverage.
6. Chat history hydration has a deferred-list, same-ID local-precedence regression test.
7. Current-worktree packaged arm64 launch/onboarding/restart/CSP smoke has completed.

## Remaining external or human acceptance gaps

1. Signed/notarized macOS and signed Windows release artifacts require CI release credentials and a release tag.
2. Paid provider chat/media/audio/video flows require funded credentials and explicit paid-call authorization.
3. Two-device encrypted sync/recovery requires a multi-device acceptance environment.
4. Full headed keyboard, screen-reader, zoom, reduced-motion, and native-speaker locale review remains human acceptance work.

The remaining items are not used to claim a confirmed local code defect. They continue to constrain unconditional release-readiness language.
