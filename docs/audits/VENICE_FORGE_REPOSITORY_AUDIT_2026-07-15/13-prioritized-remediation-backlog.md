# Prioritized Remediation Backlog

The open subset is mirrored into `docs/ROADMAP.md`, which remains authoritative.

- [ ] **VF-AUDIT-001 — P1 — Enforce documented GitHub repository safeguards**
  - Scope/owner: GitHub settings; repository administrators; complexity S.
  - Dependencies: admin permission and selection of current required checks.
  - Acceptance: `main` requires reviews and current CI checks; force push/deletion disabled; Actions default token permission is read; jobs retain only explicit writes.
  - Verification: `gh api repos/spearchucker667/Venice_Forge/branches/main/protection` and `gh api repos/spearchucker667/Venice_Forge/actions/permissions/workflow`.

- [ ] **VF-VERIFY-005 — P1 — Complete signed and credentialed release evidence**
  - Scope/owner: macOS/Windows release, paid API, two-device sync and accessibility QA; release/QA; complexity L.
  - Dependencies: signing/notarization credentials, Windows runner, funded API account, two devices, assistive-technology access.
  - Acceptance: exact-commit evidence for signed clean install/update, secure key custody, paid generation, restart recovery, sync and the manual accessibility matrix without secret capture.
  - Verification: platform release checklist in `docs/reports/INTENDED_FEATURE_VERIFICATION_2026-07-15.md`.

- [ ] **VF-AUDIT-005 — P3 — Remove two obsolete root debug probes after owner review**
  - Scope/owner: `debug-webcrypto.cjs`, `test_playwright.js`; maintenance; complexity S.
  - Dependencies: owner approval for deletion.
  - Acceptance: both paths deleted; no required workflow/package/runtime reference; archive-clean, contracts and CI pass.
  - Verification: `npm run verify:archive-clean && npm run ci`.

- [ ] **VF-AUDIT-006 — P3 — Plan direct dependency refreshes in bounded batches**
  - Scope/owner: direct npm dependencies and build tooling; maintenance/release; complexity M.
  - Dependencies: release window and platform packaging capacity.
  - Acceptance: patch/minor updates first; majors separately; lockfile generated by supported npm; CI, audit and platform packaging pass.
  - Verification: `npm ci && npm audit --audit-level=moderate && npm run ci` plus platform jobs.

## Closed During This Audit

- [x] `VF-AUDIT-002` — descriptor-based no-follow TTS reads plus `VERIFY-126`.
- [x] `VF-AUDIT-003` — redacted CharacterLibrary exception display and typed response extraction.
- [x] `VF-AUDIT-004` — corrected root/CodeQL guidance and added agent-doc regressions.
