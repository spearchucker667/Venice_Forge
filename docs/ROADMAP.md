# Venice Forge Roadmap

This is the canonical ledger for current unfinished work only. Closed execution history and validation evidence belong in `docs/summary_of_work.md`; retained scan reports are audit input, not current status authority.

## Current Work

`VF-AUD-C6-P1-001 — Fix packaged-CSP smoke probe; restore green hosted CI — CLOSED (hosted-verified 2026-09-13).` The 2026-09-12/13 audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12-c6d9bed/`) confirmed the packaged-launch smoke added in `bb29350e` probes eval-blocking via `page.evaluate` (CDP-exempt from page CSP), failing all three hosted `electron-smoke-*` jobs on every commit (run 34743024816). The probe was replaced with a page-context inline event-handler vector and verified against a locally packaged arm64 build (3/3 smoke tests PASS). Remaining acceptance: push and confirm all three hosted `electron-smoke-*` jobs green on the new SHA — **met: hosted CI run 34756782691 on `067dca58` is 11/11 jobs success (all three `electron-smoke-*` green). Publication required one additional fix: the hosted `contracts`/`coverage` jobs exposed a latent `scenario-store.test.ts` cross-test contamination flake (fake-indexeddb save landing after `reset()` → "expected 2, received 3"); fixed in `067dca58` by draining pending macrotasks between the two resets.** Secondary: `VF-AUD-C6-P3-001` capability-token expired-entry reaping — implemented with threshold sweep + regression tests (`customProtocolAccess.test.ts` 21/21). `VF-AUD-C6-DR-001` atomic-replace consolidation — implemented: all remaining durable main-process writers now use `atomicReplaceFile()`/`atomicReplaceFileSync()` (utility gained fsync option + sync variant); `verify-backup-sync` guard patterns updated to the canonical call.

`VF-EXTERNAL-RELEASE-ACCEPTANCE — External and human release evidence (open).` The 2026-09-10 and 2026-09-11 audit findings are locally remediated; evidence packages remain under `docs/audits/`. The canonical `npm run ci`, strict i18n content validation, production HTTP probe, arm64 packaging, packaged Electron smoke, and artifact verification pass. Remaining acceptance requires: exact-SHA hosted CI/CodeQL after publication; signed/notarized macOS and signed Windows release artifacts; explicitly authorized funded-provider calls; two-device sync/recovery; qualified native-language review; and full headed accessibility review (Rules01 administrator synchronization was completed on 2026-09-11 via `scripts/enforce-github-rules.sh`). Optional video enhancement UI remains scoped out until a dedicated enhancement-model surface is approved.

`VF-GENERATION-CONTRACT-PARITY-2026-09-01 — Generation API follow-up after end-to-end audit (open).` Image Studio, image tools, video/music queueing, TTS, Electron/web transports, durable paid-queue submission, error propagation, and current official wire schemas were reconciled on 2026-09-01. Confirmed local defects (Unicode paid-queue failure, reversible prompt-bearing fingerprints, unconditional variants UI, and lost upstream string details) are implemented and verified in `docs/summary_of_work.md`. Workflow image nodes now resolve live `/models` constraints (item 1). The tracked Swagger snapshot and source manifest were refreshed to official `veniceai/api-docs` `main` `2f0f7e41` / `20260911.010226` (item 2). Item 3 scoped 2026-09-11: quote/queue builders already emit optional `upscale_factor` when a caller supplies `1|2|4`. Enhancement-only fields (`enhancement_model`, `target_fps`, softness/creativity/realism/sharp/compression/noise/halo/grain/recover_detail, `h264_output`, `output_format` mp4|prores, `slowdown_factor`) apply only to provider enhancement/upscale models advertised via GET `/models` `constraints.topaz`. Video Studio has no enhancement/upscale model surface, so those fields stay off the default text/image-to-video queue body. A later work order may add an enhancement path gated on live model constraints. The confirmed `wai-Illustrious` 500 is an external Venice worker failure and must remain classified as external until a current minimal-body replay returns differently.

`VF-THEME-ENGINE-V2-2026-08-31 — Theme Engine V2 family model, YAML pipeline, and persistence (committed 2026-08-31, `26807891`).` Core type/schema/resolver/registry work, built-in family conversion, YAML V2 pipeline (`src/theme/yaml/{parse,validate,normalize,serialize}.ts`), family-centric `ThemeMaker.tsx` editing, Electron main-process V2 persistence, and theme test/verifier updates are committed. Deliverables pass: lint, typecheck, `verify:theme-tokens`, `verify:i18n`, `verify:i18n-hardcoded-regressions`, `test:unit:theme`, ThemeMaker tests, theme service/config handler tests, and full `npm test`. Remaining deferred work: visual review and refinement of generated companion variants in `scripts/generate-theme-families.cjs`; further semantic CSS cleanup of debug-categorical colors and static dark surface scale is optional polish.

`VF-VERTEX-FULL-OAUTH-2026-08-31 — Deferred Google Vertex AI full OAuth/service-account implementation (open; design preserved, public type narrowed 2026-08-31).` VF-AUD-20260831-P2-007 closed the previously selectable but guaranteed-failing `authMode: "full"` branch in the renderer and IPC layer. `GoogleVertexConfig` now narrows to `{ authMode: "express"; apiKey: string }`; the unreachable full-mode rejection in `extractGoogleVertexConfig` and the full-mode UI in `ProvidersPanel.tsx` are removed; the prior "rejects full mode" provider-adapter test is removed. The deferred design specification remains authoritative at `docs/superpowers/specs/2026-08-24-deferred-provider-integration-design.md`; full-mode custody (service-account JSON parsing, OAuth token mint/refresh, project/location validation, main-process token endpoint selection) is intentionally not implemented in this pass and is gated on a separate work order.

`VF-CAPABILITY-PROVENANCE-2026-08-31 — Custom-protocol capability tokens (implemented).` Renderer display URLs are issued via `app:media:issueCapabilityUrl` / `desktopMedia.resolveUrl` / `resolvePlayableMediaUrl`. `venice-media`, `venice-tts`, and `venice-character-cache` handlers verify a short-lived in-memory token bound to `{ objectId, profileId, sessionId }`. Tokens are revoked on profile switch, renderer destroy, and shutdown. Origin/referer checks remain defense-in-depth. Stable persisted URLs stay tokenless.

`VF-FSM-CLASSIFIER-2026-08-31 — Semantic media classifier backend decision recorded; implementation deferred.` VF-AUD-20260831-P2-009 added `getClassifierCapabilities()` to `src/shared/safety/mediaScreener.ts`. The current production behavior is reported as `semanticImageClassifier: "unavailable" | semanticAudioClassifier: "unavailable" | semanticVideoClassifier: "unavailable"` because no ML backend is registered. The structural heuristic (tracking-pixel, MIME mismatch, magic-byte verification) is the only gate. The decision record at `docs/audits/Records/semantic-media-classifier-decision-2026-09-01.md` selects a **local on-device image classifier** as the canonical next backend, preserving the local-first privacy model and avoiding new network boundaries. Provider-side classification is reserved for re-evaluation only if compliance or accuracy requirements override those advantages. Audio and video semantic classification remain deferred until a mature open or first-party option exists. Implementation is still gated on model/runtime selection, bundle-size acceptance, and supply-chain review.

`VF-EXTERNAL-RELEASE-ACCEPTANCE-2026-08-31` — External release acceptance matrix (BETA/INCOMPLETE; cannot be closed from the local tree). Signed/notarized production artifacts, paid provider operations, multi-device sync, headed manual a11y QA, and native-language review are tracked under `VF-VERIFY-005`. Lint/typecheck/tests/coverage/contracts/build, CodeQL, and exact-SHA packaged smoke are now green. External acceptance remains intentionally NOT complete until signed/paid/two-device/headed evidence is produced on a real publication tag. VF-AUD-20260831-P2-004 added `xcrun stapler validate` and `spctl -a` to the macOS release job, `Get-AuthenticodeSignature` to the Windows release job, and a workflow-generated `release-evidence/` artifact (manifest.json, per-platform signature files, checksums.sha256, metadata.json) uploaded as a workflow artifact and attached to the draft release. The portable Windows executable is explicitly handled (it surfaces a warning instead of failing the job when unsigned). VF-AUD-20260831-P2-011 added failure-only smoke diagnostics; the 2026-09-01 repair routes all platforms through a standalone sanitized writer that records repository-relative paths and never loads TypeScript through ordinary Node.

External signed / paid / two-device / screen-reader release evidence remains the only release blocker (currently BETA/INCOMPLETE):

1. **VF-I18N-NATIVE-REVIEW-001** — **OPEN.** All non-English catalogs remain `first-pass-machine` per `docs/i18n/native-review-status.json`. Structural key coverage is complete, but native-speaker/qualified-human review has not been performed and the locales must not be marked `isProductionComplete: true` until `native-review-status.json` records a reviewer and date.

## Active Audit Reconciliation

The most recent local audit lives at `docs/audits/Records/Venice_Forge-audit-evidence-20260717-031029/EVIDENCE_MANIFEST.md`. Its twelve findings (`VF-SCAN-20260717-031029-001..012`) are tracked here; session evidence, regression-guard diffs, and validation results belong in `docs/summary_of_work.md`. Status here reflects only the live tree; audit text is immutable.

- **P3 bounded debt — still open.**
  - `VF-SCAN-20260717-031029-010..012` (documentation trackers): tracked only via `docs/summary_of_work.md` Open TODO Ledger; no code change required.
  - `VF-GOVERNANCE-2026-08-25-002` (Commit Provenance): improve future traceability with meaningful commit subjects and signed commits/tags where practical.

## Audit Input

- The 2026-09-12 current-`main` re-audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12-current-main/`) found 55 items (13 P1). Remediation closed all 13 P1s, implementable P2s, and the confirmed P3s listed in `REMEDIATION_STATUS.md`, including Replicate image dispatch, visual-workflow persist-cap warnings, and packaged CSP smoke auto-run when a binary exists. `VF-CAPABILITY-PROVENANCE-2026-08-31` is implemented. Packaged CSP/onboarding smokes still skip in `npm test` when no packaged executable is present.
- The 2026-09-12 exhaustive audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12/`) was fully remediated on `main` (all 4 P1, 6 P2, 5 P3, 2 DR, and 3 TG items resolved and verified with regression tests). Detailed validation and execution history are recorded in `docs/summary_of_work.md`.
- The 2026-07-17 03:10 deep scan was fully remediated. The current task listing in this roadmap is the only live truth surface for the audit's findings.
- Prior audit evidence bundles (`Venice_Forge-audit-results-20260716-224749`) live under the `docs/audits/Records/` history directory and are no longer authoritative retained evidence.
- `VF-SCAN-20260716-001..024` were reconciled in the 2026-07-16 current-commit session recorded in `docs/summary_of_work.md`.
- `VF-SCAN-20260717-001..012` from the prior local audit were closed in the 2026-07-17 prior session (`docs/summary_of_work.md`), subject to the bounded debt inventory above. Findings `010` and `011` remain accepted incremental architecture guidance rather than runtime defects.
- `VF-SCAN-20260717-013..016` remain product/release scope rather than hidden local defects. Deferred providers/transports stay fail-closed, orphan deletion stays analysis-only pending quarantine/rollback, and signed/paid/two-device/headed proof remains represented only by `VF-VERIFY-005`.
- `VF-AUDIT-001` was completed through the live GitHub APIs on 2026-07-16: `main` is protected with strict required checks, review/code-owner/last-push approval, admin enforcement and conversation resolution.
- `VF-AUDIT-006` was completed in bounded runtime/toolchain batches (Express 5, dotenv 17, proxy middleware 4, PDF.js 6, Electron 43, ESLint 10, React Hooks ESLint 7, globals 17, Vite 8 and the React Vite plugin 6). `@types/node` intentionally stays on 22 to match the runtime and TypeScript stays on 5.8 because `typescript-eslint` 8.64 declares `<6.1.0`. `npm outdated` showing Node 26 types or TypeScript 7 does not constitute an applicable update for this project.
