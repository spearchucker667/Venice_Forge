# Venice Forge Roadmap

This is the canonical ledger for current unfinished work only. Closed execution history and validation evidence belong in `docs/summary_of_work.md`; retained scan reports are audit input, not current status authority.

## Current State (machine-readable; refresh per session — VF-AUD-20260916-P3-002)

```text
repository_head_sha: 8fa56281 (venice-media capability token refresh on Save As)
application_code_sha: 8fa56281
verified_against_sha: 8fa56281
verified_at:         2026-09-18 (Pacific)
package_version:     3.0.0-beta.3
node_engine:         >=22.15.0 <23.0.0
npm_engine:          >=10.0.0
branch:              main
working_tree:        clean
ci_status:           success for 8fa56281 (run 35391667110 — 11/11 jobs: contracts, unit-and-integration-tests, windows-sensitive, lint-and-typecheck, macos-sensitive, coverage, script-coverage, build, electron-smoke-macos/linux/windows)
codeql_status:       success for 8fa56281 (run 35391667137)
open_findings:       VF-20260918-P2-016, VF-20260918-P3-020
external_acceptance_outstanding:
  - hosted CI/CodeQL against the published SHA
  - headed accessibility/visual QA (per-tab acceptance, P2-016)
  - native-language translation review of the 12 non-English catalogs
  - funded-provider verification of newly-wired paths (Responses, x402, Crypto RPC)
recently_closed_in_session_2026-09-18:
  - VF-20260918-AUDIT-CLOSURE: the 2026-09-18 exhaustive review and remediation handoff is closed; 18 of 20 findings shipped in commits 07222274 through ee6ade04, handoff moved from docs/audits/TODO/ to docs/audits/Records/, the four pre-existing stale Markdown-link failures from the user-owned audit moves were repaired, and the 6 leftover tests in server.test.ts / electron/ipc/handlers.test.ts that still encoded the OLD "mandatory child safety when optional filter is off" contract were aligned to the chosen disabled-mode policy (skipped). Published main HEAD 53efc703.
  - VF-20260918-P0-001, P1-002, P1-003, P1-004, P1-006, P2-007 (commit 1c0360f8) Local Family Safe Mode disabled-state contract aligned across renderer, Electron, web proxy, RP/import, response screening, diagnostics, tests, and active documentation
  - VF-20260918-P1-005 (commit e34f292a) Fuzzy-only safety collisions downgraded to warning-only signals; benign corpus coverage added
  - VF-20260918-P2-008 (commit 07222274) Shared ContextMenu viewport bounds, keyboard movement, disabled-item skip, and focus restoration
  - VF-20260918-P2-009 (commit 57efe436) Sidebar chat-options menu migrated to shared ContextMenu
  - VF-20260918-P2-010 (commit 72fdff4b) History folder menu migrated to shared ContextMenu
  - VF-20260918-P2-011, P2-012, P2-013 (commit 4ce828f6) Shared Select active-descendant IDs, filter bounds, and viewport placement
  - VF-20260918-P2-014, P2-015 (commit 7bf1defa) Responsive sidebar labels and Character Library action rows
  - VF-20260918-P3-019 (commit 5eca2e45) ContextMenu added to Superdesign governance with refreshed fingerprint
  - VF-20260918-P2-017 (commit 80fb45b1) Hosted Rules01 governance verified (13 required contexts)
  - VF-20260918-P3-018 (commit ee6ade04) Current-state docs lag with separate code/verified SHAs
  - VF-AUD-20260916-P2-004 (commit 1c2bfe1d)  Google API-key query-string + redaction gap
  - VF-AUD-20260916-P2-005 (no new commit)   Family-safe media heap pressure (verified closed by VF-20260916-P1-003)
  - VF-AUD-20260916-P3-002 (commit 6d577eba) Current-authority docs reconciliation
  - VF-AUD-20260916-P2-002 (commit 59a6bf7c) Selector↔compiler envelope-overhead invariant
  - VF-AUD-20260916-P2-001 (commit a64e5a7d) Superdesign source drift verifier + init refresh
  - VF-AUD-20260916-P2-006 (commit 0dcd97a3) Truthful media-classifier capability surfaced to Status
```

## 2026-09-18 Audit Work Order

The 2026-09-18 current-`main` exhaustive review and remediation handoff is closed and now lives at `docs/audits/Records/VENICE_FORGE_CURRENT_MAIN_EXHAUSTIVE_REVIEW_AGENT_HANDOFF_2026-09-18.md` (published `main` HEAD `b0f9f6a5`). All 18 actionable findings were repaired in commits `07222274` through `ee6ade04`:

| Finding | Source commit |
|---|---|
| `VF-20260918-P0-001` | `1c0360f8` |
| `VF-20260918-P1-002` | `1c0360f8` |
| `VF-20260918-P1-003` | `1c0360f8` |
| `VF-20260918-P1-004` | `1c0360f8` |
| `VF-20260918-P1-005` | `e34f292a` |
| `VF-20260918-P1-006` | `1c0360f8` |
| `VF-20260918-P2-007` | `1c0360f8` |
| `VF-20260918-P2-008` | `07222274` |
| `VF-20260918-P2-009` | `57efe436` |
| `VF-20260918-P2-010` | `72fdff4b` |
| `VF-20260918-P2-011` | `4ce828f6` |
| `VF-20260918-P2-012` | `4ce828f6` |
| `VF-20260918-P2-013` | `4ce828f6` |
| `VF-20260918-P2-014` | `7bf1defa` |
| `VF-20260918-P2-015` | `7bf1defa` |
| `VF-20260918-P2-017` | `80fb45b1` |
| `VF-20260918-P3-018` | `ee6ade04` |
| `VF-20260918-P3-019` | `5eca2e45` |

Two findings (`P2-016` headed visual/accessibility QA and `P3-020` qualified native-language review) require external human acceptance and remain blocked. **Scaffolding + tooling only** has been prepared to make the human review tractable:

- `VF-20260918-P2-016` tooling (published `main` HEAD `b6618b6f`):
  - `docs/design/per-tab-acceptance/README.md` (canonical surface + coverage math)
  - `docs/design/per-tab-acceptance/CHECKLIST.md` (per-tab reviewer checklist)
  - `docs/design/per-tab-acceptance/EVIDENCE_MANIFEST.template.json` (JSON schema with reviewer.signature + contact)
  - `scripts/per-tab-acceptance/tab-routes.json` (15 tabs × route + initial focus + primary action)
  - `scripts/per-tab-acceptance/runner.sh` (idempotent Playwright stub generator via the local Codex skill wrapper)
  - `scripts/verify-per-tab-acceptance.cjs` (verifier; exits non-zero until every required tuple has a signed manifest + non-empty notes.md)
  - `npm run verify:per-tab-acceptance`
- `VF-20260918-P3-020` tooling (published `main` HEAD `b6618b6f`):
  - `docs/i18n/review-pack/README.md` (per-locale scaffolding + policy)
  - `docs/i18n/review-pack/PLACEHOLDER-INVENTORY.json` (machine-readable; 3,916 placeholders across 11 non-English locales × 12 namespaces)
  - `docs/i18n/review-pack/PER-LOCALE/<locale>.md` × 11 (per-locale checklist)
  - `scripts/generate-i18n-review-pack.cjs` (regenerator)
  - `scripts/verify-i18n-review-status.cjs` (verifier; refuses to mark a locale human-reviewed while placeholders remain)
  - `npm run generate:i18n-review-pack` and `npm run verify:i18n-review-status`

Both findings remain `OPEN` in `open_findings`; closure requires out-of-band human work and the verifier-driven evidence described above.

## Current Work

`VF-AUD-20260916-P2-007 — Direct headed a11y/visual QA remains a separate release task (no commit).` Per `docs/audits/TODO/VENICE_FORGE_CURRENT_MAIN_DEEP_AUDIT_AGENT_HANDOFF_2026-09-16.md` §VF-AUD-20260916-P2-007, the audit requires headed Chromium accessibility / visual QA with screen-reader and visual confirmation across **15 canonical tabs without direct dedicated surface evidence**: `character-chats, history, image-inspector, prompts, scenes, audio, music, video, embeddings, search, characters, character-creator, rp-studio, privacy, playground`. The existing reference-redesign evidence captures 36 sessions across 9 surface families, 4 presets, and 5 viewport categories — useful but not per-tab. **Not executable from the current headless environment**: this agent session has no headed browser, no screen reader, and no manual visual review capability. The audit's required repair is a separate release-time task that needs a human reviewer running Venice Forge in headed mode and recording per-tab acceptance evidence under `docs/design/reference-ui-redesign-evidence/`. **No code change in this commit** — the audit entry remains `P2-007` in `open_findings` until that evidence is recorded. All other audit findings from the 2026-09-16 deep audit handoff are now closed (P2-001, P2-002, P2-004, P2-005, P2-006, P3-002) on `main` baseline `0dcd97a3`.

`VF-AUD-20260916-P2-004 — Closed the Google API-key query-string + redaction gap on `main` baseline `c8fa1a5e` (audit-tranche 2026-09-17 session, commit `1c2bfe1d`).` Per `docs/audits/TODO/VENICE_FORGE_CURRENT_MAIN_DEEP_AUDIT_AGENT_HANDOFF_2026-09-16.md` finding §VF-AUD-20260916-P2-004. Two distinct surfaces fixed. **(a) URL credential leakage** — `electron/ipc/handlers/apiKeyHandlers.ts` `buildProviderTestRequest()` no longer embeds the Gemini or Vertex Express API key as `?key=...` in the test URL; both now carry the credential in the `x-goog-api-key` header (matches the documented Gemini REST contract and mirrors `electron/services/providerAdapters.ts` for the request transport). The Vertex branch's inline comment explains the rationale so the next reviewer does not revert to the query form. The previously-bare `apiKey: ""` case now emits an empty headers object instead of a `?key=` query, preserving the no-credential path. **(b) Redaction gap** — `src/shared/redaction.ts` `redactUrl()` now masks `key`, `apiKey`, `api_key`, `api-key`, and `x-goog-api-key` query parameter values via the new `SENSITIVE_URL_QUERY_NAMES` set (case-insensitive lookup). Lookup is NOT generalized to "every field named `key` is secret" — that path is explicitly rejected per audit guidance, and the test suite asserts an ordinary `?page_key=p1` survives unchanged. `sanitizeErrorText` automatically propagates the fix via `redactPaths` → `redactUrl`. **Tests added**: new `electron/ipc/handlers/apiKeyHandlers.connection.test.ts` (5 tests covering Gemini + Vertex Express + bearer-header sanity for Anthropic/Azure); `src/shared/redaction.test.ts` gains a 9-test `redactUrl` suite (each credential query name masked, non-credential params preserved, fragment stripped, embedded credentials redacted, narrow-by-design verified, end-to-end diagnostic-string path). `buildProviderTestRequest` is now exported with a JSDoc note explaining the unit-test contract (same pattern as `isReservedCredentialName`). **Validation**: `npm run lint:eslint` 0/0, `npm run typecheck` 3/3 tsconfigs, focused vitest 31/31, broader vitest 189/189 across 16 files (`src/shared` + `electron/ipc`). **Remaining audit work (unchanged)**: `P2-001` Superdesign source drift, `P2-002` attachment envelope budgeting, `P2-005` family-safe media heap (likely closed by `VF-20260916-P1-003` — to verify on next pass), `P3-002` current-authority docs, `P2-006` semantic media-safety capability, `P2-007` direct headed visual/a11y acceptance (separate release task). 

`VF-VENICE-API-2026-09-16-PHASE-6-RESPONSES-2026-09-17 — Responses API opt-in experimental transport (Phase 6 / Phase 8 of the feature-gap handoff) on `main` (2026-09-17 session).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md` §13. **POST /responses experimental transport**: implemented as an explicit, opt-in, experimental chat transport that NEVER silently switches `/chat/completions` off default. Shared canonical types and SSE parsing in `src/shared/veniceResponses.ts` (`VeniceResponsesRequest`, `VeniceResponsesEvent`, `ResponsesOutputBlock`, `extractResponsesEventScreenText`, `extractResponsesBodyScreenText`, `modelSupportsResponsesApi`). Strict endpoint validation (`ALLOWED_VENICE_ENDPOINTS` includes `/responses` with POST-only method gate; no wildcard routing). Shared renderer transport in `src/services/veniceClient/responses.ts` (`veniceStreamResponses`) with 5-minute timeout, full telemetry row, and renderer safety guard for web mode. Chat stream manager integration in `chat-stream-manager.ts`: new chat turns route to `veniceStreamResponses` only when `responsesApiEnabled` setting is active AND the selected model is confirmed non-E2EE (`modelSupportsResponsesApi`); fails closed to `/chat/completions` when unsupported (tool-turn history, input_audio content parts, E2EE models, or missing metadata). Main process streaming support in `electron/ipc/handlers/veniceHandlers.ts` and `electron/services/veniceClient.ts` with authoritative FSM safety pipeline; web proxy support in `server.ts` with per-event and whole-body FSM screening (`extractResponsesEventScreenText`/`extractResponsesBodyScreenText`). Safety provenance in `src/services/ingestion/xmlEscape.ts` unified across both `messages` (chat) and `input` (responses). Settings UI: new `DeveloperPanel.tsx` in `SettingsView.tsx` with experimental badge, model eligibility indicator, and toggle defaulting to off. i18n: `settings:developer.responsesApi.*` keys added in `en-US` and synchronized to 11 locales. Validation: 368 focused tests pass (11 files), typecheck clean (3 tsconfigs), ESLint 0 warnings, verify:contracts:static (all 25 checks pass), verify:i18n and verify:i18n-hardcoded-regressions pass.

`VF-VENICE-API-2026-09-16-PHASE-5-SEEDANCE-2026-09-17 — Seedance video contract parity (Phase 5 of the feature-gap handoff) on `main` (2026-09-17 session).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md`, against the freshly synced upstream mirror (HEAD `8989e40`, 2026-09-17). **5.1 `bitrate_mode` — IMPLEMENTED** (the 2026-09-16 "no evidence" verdict is reversed by the newer mirror): guide-level contract (`guides/media/seedance-2-0.mdx`, `guides/media/video-generation.mdx`) documents `standard` (default, same as omitting) / `high` for Seedance 2.0 (incl. Fast) and 2.5, queue-only, no price change, and `/video/quote` does NOT accept it. The vendored swagger does not declare the field, so the Seedance-family pattern (`supportsVideoBitrateMode()`, fail-closed) is the only documented discriminator; a test asserts the vendored swagger remains undeclared so future spec pickups are caught. Builder emits `bitrate_mode: 'high'` only; Video Studio gained a gated "Output bitrate" select reset on model switch. **5.2 source-matched duration/aspect — IMPLEMENTED** at the contract/builder layer: `-1`/`auto` duration and `adaptive`/`auto` aspect enums exist in the committed vendored swagger (`QueueVideoRequest`/`QuoteVideoRequest`), with the documented required companions (`reference_video_urls` for queue, `reference_video_total_duration` for quote — bills `ceil(...)` seconds); builders throw when the pairing is violated. The interactive Video Studio filters these tokens out (it never attaches reference videos, so upstream would 400); full R2V support is follow-up below. **5.4 xAI image-driven quote reconciliation — DEFERRED-NO-EVIDENCE (verified, not assumed)**: the `QuoteVideoRequest` schema carries no image-count/input-cost field, `overview/pricing.mdx` and `models/video.mdx` route Grok Imagine pricing to the Video Quote API ("Variable — use Video Quote API"), and no per-image billing language exists anywhere in `guides/media/*.mdx`. The quote endpoint is already the app's authoritative price source, so there is nothing to wire. Validation: typecheck (3 projects), lint 0/0, verify:i18n + hardcoded-regressions, 62+ focused contract tests including a vendored-swagger cross-assertion, verify:venice-contract-drift PASS. **Follow-up gap (wire evidence exists, out of phase scope)**: `omni_reference_task_type` (Seedance 2.5 R2V task-type hint, optional — Venice infers when omitted) and a full R2V reference-media UI (`reference_video_urls` inputs in Video Studio) belong to a future R2V phase. Open: native-language translation of new keys, headed QA, hosted CI against the published SHA, funded-provider verification of `bitrate_mode: 'high'` output.

`FEAT-006-P2-006-PHASE-4B-2026-09-17 — Native file/video chat inputs + prompt-limit enforcement wired into the renderer on `main` baseline `a85866fa` (Phase 4B of the 2026-09-16 feature-completion handoff).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md`. **FEAT-006 renderer wiring**: per-attachment source/mode choice (Local context / Native file / Native video) in the chat composer with submit-time canonical validation (`validateContentParts`) that blocks the send and annotates the offending attachment; raw local filesystem paths can never reach the wire; native-file payloads capped at `VENICE_MAX_RAW_UPLOAD_BYTES` with localized guidance toward the local-context path. Giant base64 payloads are never persisted — the bounded `nativeContentPartRegistry` holds them at runtime, the durable message keeps only `metadata.nativeParts` refs, and `compileChatPrompt` expands them provider-side via `CompileChatPromptOptions.resolveNativePart` (wired in `chat-stream-manager.ts`); post-restart refs resolve to null and the part is omitted from new requests without touching history. No runtime model-capability flag exists for native file/video inputs, so the modes are offered for every model with the canonical validator as gatekeeper (documented in code). **VF-20260916-P2-006 enforcement**: `PromptLimitMeter` shows count vs the effective per-model limit (`resolvePromptCharacterLimit`) in the image/video/music studios, and Generate is blocked (disabled button + localized message) while over limit; the image studio no longer silently truncates interactive input (gallery-imported drafts keep the compatibility slice). New `chat:composer.*` and `media:promptLimit.*` keys in `en-US`, synced to 11 locales with `__MISSING__:` placeholders. Validation: typecheck (3 projects), lint:eslint 0/0, verify:i18n, verify:i18n-hardcoded-regressions, mandated safety/chat suites (291 tests), 37 new focused tests, broader sweeps (332 + 898 tests) — all green. Open: native-language translation, headed accessibility/visual QA, hosted CI/CodeQL against the published SHA, funded-provider end-to-end. **Uncommitted on `main`, not pushed.**
`VF-AUD-P1-ENVELOPE-SPLIT-HARDENING — Closed the body-text envelope-split vector in `buildExternalAttachmentEnvelope` on `main` baseline `fa9fd9ad` (fifth slice in 2026-09-16 session).` Per `docs/audits/Records/VENICE_FORGE_CURRENT_MAIN_DEEP_AUDIT_AGENT_HANDOFF_2026-09-14.md` finding §5 (P1 — Attachment Provenance Envelope Is Not Safely Serialized). **Reproduced the exploit**: prior code allowed attachment body text containing `</external_attachment><external_attachment id="evil">…` to split the safety-guard envelope into TWO quoted segments, leaking the attacker's text into a separate quoted-attachment pool. The regex's non-greedy match closed on the first occurrence; the second envelope's content was classified as quoted attachment data, NOT user instruction. **Fix**: new `neutralizeEnvelopeTagsInBody()` in `src/services/ingestion/xmlEscape.ts` replaces any literal `<external_attachment>` or `</external_attachment>` substring inside body text with `[external-attachment-tag-redacted]` before the envelope is serialized. Body content is otherwise preserved verbatim so the safety guard still sees the literal text for classification (CSAM hard-block invariant preserved). The placeholder is human-readable so audit logs and the safety guard's classification pass see the sanitized text rather than the raw attack payload. **Tests added**: 3 new tests in `xmlEscape.test.ts` (closing-tag injection, opening-tag injection, benign-content preservation); 3 tests for the `neutralizeEnvelopeTagsInBody` helper; 1 end-to-end regression test (VF-CUR-P1-002-b) in `childExploitationGuard.test.ts` proving the full buildExternalAttachmentEnvelope → splitExternalAttachmentSegments pipeline produces exactly ONE quoted segment regardless of how many literal closing tags the body contains. 182 focused tests pass (was 175; +7 new). **Architectural follow-up tracked for future refactor** — handoff §5 also recommends stopping the regex-parsing-of-serialized-prompt pattern entirely and routing typed segments through the safety guard. That is a larger refactor across `use-chat.ts` → `childExploitationGuard.ts` → `assessChildExploitationSafety` consumers; this commit closes the specific P1 split-envelope vector only. Logged as `VF-AUD-P1-002-STRUCTURAL` below.

`VF-VENICE-API-2026-09-16-FEATURE-GAP-P3-TRANCHE — Venice API feature-gap P3 tranche (Phases 12 + 5.3) on `main` baseline `613d2d26` (fourth slice in 2026-09-16 session).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md`. **Phase 12 — Character reviews**: extended `isAllowedCharactersRequest()` in `src/shared/validation.ts` to accept `/characters/{slug}/reviews` as the *single* allowed nested route under the `/characters` family (handoff §17 forbids generalizing to arbitrary nested paths). New `CHARACTER_REVIEWS_SUFFIX = '/reviews'` constant and `extractCharacterSlugFromReviewsPath()` helper; 7 new validation tests cover GET-only acceptance, the no-other-nested-paths rule, slug-validation propagation, and the constant alignment. **Phase 5.3 — Per-model prompt character limit**: added `resolvePromptCharacterLimit(modelInfo, modality, fallback)` resolver in `payloadBuilders.ts` honoring the handoff's "use live model metadata over a single global magic number" rule. Precedence: `model.model_spec.prompt_character_limit` (authoritative) → `DEFAULT_PROMPT_CHARACTER_LIMITS[modality]` (2500 video / 500 music, sourced from Swagger descriptions) → explicit `fallback`. `DEFAULT_PROMPT_CHARACTER_LIMITS` keeps the documented image default at 7500 (the long-standing app-level clamp) because the current Swagger does not document an image default. **Did not silently truncate**: the resolver surfaces a documented limit but the rendering layer is responsible for *enforcement* — `buildImagePayload()` keeps the legacy `slice(0, VENICE_IMAGE_MAX_PROMPT_CHARS)` behavior for back-compat; new callers should validate before invoking the builder (see handoff §10.3 "do not silently truncate"). **Sub-deliberately deferred** (no upstream Swagger evidence per §31): Phase 5.1 `bitrate_mode`, 5.2 source-match Seedance options, 5.4 input-image pricing (the "per input image" pricing for xAI video is not documented in current Swagger and the existing `VideoQuoteWirePayload` shape does not carry an image-count field), Phase 13 optional investigations (Agent tools discovery, etc.), Phase 8 Responses API, Phase 10 x402 wallet auth, Phase 11 Crypto RPC — each gated per handoff §28 Do-Not-Rules. UI surfaces for character reviews (Settings → Providers → Venice → API Keys / character detail drawer) and the prompt-length counter update remain separate renderer workstreams. Validation: typecheck (root + Electron + Electron test), `lint:eslint --max-warnings=0`, `verify:repo-handoff-hygiene`, `verify:contracts:static` (every gate green), `verify:i18n`, `verify:markdown-links`, 128 focused tests in `validation` + `payloadBuilders` — all green.

`VF-VENICE-API-2026-09-16-FEATURE-GAP-P2-TRANCHE — Venice API feature-gap P2 tranche (Phases 6/7/9) on `main` baseline `a79b8a62` (third slice in 2026-09-16 session).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md`. **Phase 6 — Native file and video inputs for chat**: extended `ContentPart` discriminated union with `file` (`{ file_data, filename? }`) and `video_url` (`{ url }`) content types plus `CacheControlHint` for `cache_control` blocks; new `src/shared/contentPartValidation.ts` provides `looksLikeLocalFilesystemPath`, `isSupportedFileData`, `isSupportedVideoUrl`, `validateContentPart`, `validateContentParts` — fail closed with structured `ContentPartValidationError` per part. Hard rules enforced: raw local filesystem paths are rejected (never send a device path over the wire), video URLs limited to `mp4`/`mpeg`/`mov`/`webm` plus documented YouTube support, file `file_data` limited to documented MIME prefixes, max 3 video_url parts per request (verbatim from Swagger description). **Phase 7 — prompt cache retention**: added `PromptCacheRetention = 'default' | 'extended' | '24h'` and top-level `prompt_cache_retention?` on `ChatCompletionRequest`; `resolvePromptCacheRetention()` resolver in `payloadBuilders.ts`; provider-fallback path in `chat-stream-manager.ts` strips `prompt_cache_retention` for non-Venice providers. **Phase 9 — API-key administration**: opened `/api_keys` (GET/POST), `/api_keys/{id}` (GET/PUT/DELETE), `/api_keys/rate_limits` (GET), `/api_keys/rate_limits/log` (GET) on `ALLOWED_VENICE_ENDPOINTS` and `VENICE_ENDPOINT_METHODS`; new `isAllowedApiKeysRequest()` and `extractApiKeyId()` parameterized matcher; `ALLOWED_VENICE_METHODS` widened to include PUT/DELETE for single-key CRUD. **Hard constraints honored per handoff §28**: did not implement Phase 8 (Responses API) — Do-Not-Rule "do not silently switch chat→responses"; did not implement Phase 10 (x402 wallet auth) — Do-Not-Rule "do not auto-top-up balances"; did not implement Phase 11 (Crypto RPC) — Do-Not-Rule "do not allow arbitrary crypto network/path strings". UI surfaces (attachment menu with local/native/video options, Settings → Providers → Venice → API Keys, Billing cards/tables, E2EE Privacy selector) remain separate renderer workstreams.

`VF-VENICE-API-2026-09-16-FEATURE-GAP-P1-TRANCHE — Venice API feature-gap P1 tranche (Phases 2/4/5 partial) on `main` baseline `199a68c6` (committed in 2026-09-16 session, second slice).` Per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md`. **Phase 2 — Billing/Usage center** opens the canonical `/billing/balance`, `/billing/usage-history`, and `/billing/usage-analytics` endpoints on `ALLOWED_VENICE_ENDPOINTS` with GET-only `VENICE_ENDPOINT_METHODS` (no POST — billing is read-only at the IPC boundary). The deprecated `/billing/usage` is intentionally not added; new code MUST prefer `/billing/usage-history`. **Phase 4 — E2EE request selection**: added `E2eeOverride = 'provider-default' | 'on' | 'off'` and `enable_e2ee?: boolean` on `VeniceParameters`; `supportsE2EE()` capability gate in `src/shared/modelCapabilities.ts` (canonical nested-shape + back-compat hoisted-shape); `resolveE2eeParam()` enforces "never set merely because the model supports E2EE — explicit opt-in required"; provider-fallback path in `chat-stream-manager.ts` strips `enable_e2ee` alongside the existing `enable_document_tools` strip. **Phase 5.5 — TTS per-model output formats**: added `model_spec.supported_formats` and `model_spec.default_format` to `VeniceModel`; `resolveAudioResponseFormat()` enforces "explicit format outside this list is rejected by upstream" — falls through to `default_format` or conservative `'mp3'` when not on allowlist. **Deferred in this tranche**: Phase 5.1–5.4 (Seedance bitrate_mode / source-match / prompt length / input-image pricing) — Venice Swagger currently documents `model_spec.supported_formats`/`default_format` but does not document `bitrate_mode`/`prompt_character_limit`/per-image video pricing for the current Seedance snapshot. Adding these without verification would violate handoff §31 ("do not invent request or response fields"). UI surface for the new canonical capabilities remains a separate renderer workstream.

`VF-VENICE-API-2026-09-16-FEATURE-GAP-FIRST-SLICE — Venice API feature-gap handoff first slice (Phases 0/1/3) on `main` baseline `19c820ff` (committed in 2026-09-16 session, first slice).` Implemented per `docs/audits/TODO/VENICE_API_2026-09-16_FEATURE_GAP_AGENT_HANDOFF.md` §29. Phase 0 contract sync (`docs/reference/venice-api-upstream` HEAD `1993429ec`, `verify:venice-api-docs`/`verify:venice-contract-drift` PASS). Phase 1 model metadata: canonical `model_spec.uncensored` field + `resolveModelUncensored()` precedence helper (`model_spec.uncensored` → legacy `most_uncensored` trait → `false`); `useAgentModels()` rewired off the legacy trait heuristic; `/models` React Query key appended with `activeProfileId` so a profile A result cannot be served to profile B (`modelCatalogCache` confirmed in-memory only — no IDB persistence); `extractChatResponseCost()` + `estimateChatCostFromListPricing()` separate the two cost paths so caller-specific `usage.cost` is authoritative over list pricing without silent substitution. Phase 3 typed rate-limit reason: `VeniceRateLimitReason` / `VeniceRateLimitType` / `VeniceRateLimitInfo` types, `extractRateLimitInfo(headers, body)` extractor (preserves `Retry-After` + `x-ratelimit-reset-requests` semantics, normalizes the abbreviated upstream aliases), 429 path attaches `rateLimit` to `VeniceApiError`, and `resolveRateLimitMessageKey()` UX mapper (pure, no i18next dependency, usable from main-process paths). 6 new `errors` namespace keys translated across 12 locales; `verify:i18n` exit 0 (non-English locales remain `isProductionComplete: false` until qualified native review). **Deferred to subsequent sessions** (per handoff §29 / §31 — do NOT mechanically add the remaining endpoints without per-phase contract verification): Phase 5.1–5.4 (Seedance/media delta pieces without verified Swagger), Phase 6 (file/video inputs), Phase 7 (cache retention controls), Phase 8 (Responses API alpha — handoff Do-Not-Rule: do not silently switch chat→responses), Phase 9 (API-key CRUD/rate-limit endpoints), Phase 10 (x402 wallet auth), Phase 11 (Crypto RPC), Phase 12 (character reviews), Phase 13 (optional investigations).

`VF-AUD-20260916-CURRENT-MAIN — Current-main deep-audit follow-up (baseline 59b61c43).` The first safety-streaming tranche adds bounded cross-event, per-choice SSE screening and restores immediate web delivery of approved deltas; focused tests, lint, typecheck, and build pass. The full local suite has one unrelated failure because pre-existing user-owned audit-record deletions leave a broken `docs/DOCS_INDEX.md` link; contract/release-readiness checks stop at the same link. See `docs/summary_of_work.md` for exact results. Remaining audit work: Superdesign source-drift verification (`P2-001`), attachment envelope budgeting (`P2-002`), Google key transport/redaction (`P2-004`), Family-Safe media heap pressure (`P2-005`), Jina connection-test timeout/body cleanup (`P3-001`), current-authority documentation reconciliation (`P3-002`), semantic generated-media capability (`P2-006`), and direct headed accessibility/visual acceptance across canonical tabs (`P2-007`). External signing, funded-provider, two-device sync, and native-language acceptance remain separate release tasks. Revalidate each finding against current `main` before implementation.

`VF-AUD-20260915-SSE-CLIENT-COMPLETION — Missing terminator handling (uncommitted).` Real web and Electron stream entry-point tests reproduced successful 2xx SSE responses being accepted at EOF without `[DONE]`. `src/services/veniceClient/stream.ts` and `electron/services/veniceClient.ts` now reject that incomplete success as a sanitized retryable 502-style error while preserving non-2xx terminal responses. Focused client/server/SSE validation passes (8 files / 186 tests), including chat-stream-manager cleanup for incomplete streams. Final typecheck, ESLint, build, headless decoder assertion, diff checks, `npm run verify:dist`, and `npm run verify:markdown-links` pass. The repository CI-equivalent `npm run test:ci` also passes all shards: 544 files / 6,208 passed / 4 skipped. Existing i18n fallback notices and unrelated test warnings remain non-failing.

`VF-AUD-20260915-SSE-LIFECYCLE-REDESIGN — Focused transport coordinator (committed locally in this session).` Replaced the inline Family Safe Mode SSE lifecycle state machine in `server.ts` with `src/services/safetyGatedSsePump.ts`, which owns ordered async classification, one-chunk bounded buffering, upstream pause/resume, completion, and cancellation. Added direct contract tests for delayed classification ordering, upstream end during active work, overflow, and cancellation; the real proxy approval/termination test remains green. Focused server/SSE validation passes (4 files / 114 tests), along with lint, typecheck, and diff checks. Repeated proxy error/status serialization is now handled by one route-local helper. Behavioral verification later passed through the real proxy test and a headless module lifecycle assertion. An empty-upstream route probe found and fixed missing `Content-Type: text/event-stream` initialization on successful zero-event completion; focused route tests now cover that boundary. Full `npm test` passed (544 files / 6,208 passed / 4 skipped) and `npm run build` passed for web, server, and Electron. The two real-route SSE tests now share one event-list fixture, a focused real-client disconnect case confirms upstream cleanup, and the pump test uses a minimal typed EventEmitter adapter. The unsafe-route fixture now uses actual SSE line breaks. Real-route verification then found and fixed a hanging-response defect when a safety or upstream failure occurred after headers had been sent: `server.ts` now ends an already-started response instead of returning early. Route regressions cover unsafe later events, bounded overflow, upstream errors before and after output, and disconnect cleanup. The SSE fixture now uses one terminal-mode option rather than a second proxy override.

`VF-AUD-20260915-ADVERSARIAL-PASS — Boundary corrections (uncommitted).` Reproduced and corrected three concrete Family Safe Mode SSE defects (normal request `close` cancelled healthy streams, `headersSent` suppressed events after the first, and lagging classification could retain an unbounded promise/buffer chain) plus attachment admission dropping usable remaining model budget because it accepted only whole chunks. The server now distinguishes abort/disconnect cleanup, initializes SSE headers once while releasing every approved event, pauses/resumes upstream delivery with one bounded queued chunk and explicit overflow failure; attachment selection fits a Unicode-safe partial prefix and reports partial omission. Focused server, SSE, attachment-hook, ingestion, and bridge tests pass (5 files / 113 tests); parser framing cases are table-driven, async classification ordering is covered, and redundant model-budget cases were removed. Lint, typecheck, build, and diff checks pass. Full-suite, headed, funded-provider, hosted CI, and CodeQL checks remain not run for this uncommitted pass.

`VF-AUD-20260915-REMEDIATION — Current-main remediation tranche (in progress, baseline `07ad9ee6ec6e4249f2ea2a27dc794fa148bd96e8`).` Confirmed fixes now present in the worktree: privileged IPC sender validation fails closed when `senderFrame` is unavailable; Family Safe Mode chat SSE uses bounded incremental UTF-8/SSE gating; attachment extraction retains bounded provenance-carrying chunks and selects them against the selected model budget; the unsupported Jina `respectRobotsTxt` promise was removed from the provider contract; Superdesign init artifacts, agent bootstrap guidance, and duplicate theme declarations were refreshed; and offline `verify:superdesign-init` coverage was added. **Validation completed:** focused remediation tests (6 files / 57 tests), attachment/research/server tests (8 files / 143 tests), `npm run lint:eslint`, `npm run typecheck`, `npm run verify:superdesign-init`, `npm run verify:agent-docs`, and `npm run verify:theme-tokens` all pass. **Remaining acceptance:** semantic generated-media classification remains unavailable by design; headed visual/a11y acceptance remains unrun; direct evidence is currently `CAPTURED` for shell, chat, settings, status, image-studio, media-studio, workflows, documents, and command-palette, while `NOT_TESTED` for character-chats, history, image-inspector, prompts, scenes, audio, music, video, embeddings, search, characters, character-creator, RP Studio, privacy, and playground. Manual theme/locale/viewport and hosted exact-SHA validation for this uncommitted tranche remain pending.

`VF-REFERENCE-UI-REDESIGN-2026-09-14 — Reference-driven UI redesign (Phases 0–1 complete on baseline `b5760db2`; Phases 2–7 + Theme Remediation LANDED 2026-09-15 on `a61a5c3e`; Phases 8–10 in flight).` Per `docs/design/VENICE_FORGE_REFERENCE_UI_REDESIGN.md` (authoritative) and ledger `docs/design/VENICE_FORGE_REFERENCE_UI_REDESIGN_MATRIX.md`: Superdesign baselines + four approved reference-driven direction drafts exist on project `50e9d3b6-0d15-4340-8672-f378572d35ec` (shell `33368bdb`, chat `33d6ac04`, settings/theme-maker `93d23773`, status `8faeedd3`). Architecture decision: theme engine preserved byte-for-byte; new derived `--vf-*` shell material layer in `src/styles/theme.css` via `color-mix()` from existing semantic tokens; only the default Venice dark variant's token values retune toward graphite-neutral + crimson; all other families/custom/YAML themes inherit geometry with their own hue. **Phase 1 (material foundation) implemented and validated 2026-09-14** on baseline `b5760db2` (commits `db05cd50`, `e3a6fd25`, `2b77f28c`, `79b231c4`, `f5c8b69b`, `f5c8b69b`, `a77063e7`, `c4680d93`, `874a7936`): derived `--color-vf-*` material layer, graphite/crimson Venice retune, static grain ambient layer, reference material classes with contracted radii, six shell primitives; lint/typecheck/theme-token/i18n-regression verifiers, focused vitest (366/366), and `build:web` all PASS. **Phases 2–7 + Theme Remediation LANDED 2026-09-15 on `a61a5c3e` (fast-forward, no force-push):** 8 coherent commits on top of `874a7936` covering global shell (`14ebcab3`), Chat family (`3a59ca9f`), Generate family (`f6b30d67`), Build family (`cb0c90c0`), System family incl. ThemeMaker visual + themeToYaml mode round-trip fix (`cb2151cb`), Overlays/transient surfaces (`c0290ad1`), Light/Dark variant remediation + YAML mode round-trip + 39 companion contrast fixes (`55f980a1`), docs (`a61a5c3e`). Validation suite (clean worktree): `lint:eslint` 0/0, `typecheck` 3/3 tsconfig, full `npm test` 6171 passed / 4 skipped / 535 files, `npm run build` 5.33s, `verify:dist` PASS, `verify:contracts` 104/104, `verify:theme-tokens` 185/185, `verify:i18n-hardcoded-regressions` 537/537; hosted CI on `a61a5c3e` (run `34955834255`) all 11 jobs SUCCESS (`lint-and-typecheck`, `macos-sensitive-tests`, `windows-sensitive-tests`, `unit-and-integration-tests`, `coverage`, `contracts`, `script-coverage`, `build`, `electron-smoke-windows`, `electron-smoke-macos`, `electron-smoke-linux`) and CodeQL (run `34955834280`) SUCCESS. **In-flight (this session):** Phase 8 — added `tests/accessibility/reference-rtl-shell.test.tsx` (3 tests), `tests/accessibility/reference-viewport.test.tsx` (2 tests), `tests/accessibility/reference-keyboard-nav.test.tsx` (3 tests), `tests/accessibility/reference-focus-restore.test.tsx` (3 tests), `tests/accessibility/reference-aria-live.test.tsx` (2 tests); a11y test bundle now 13 tests / 7 files PASS (vs pre-redesign baseline of 5 tests / 2 files). Phase 9 — extended `scripts/capture-redesign-matrix.mjs` to the full §33 matrix: 9 surfaces (shell / chat / image-studio / media-studio / workflows / documents / settings / status / command-palette) × 4 presets (venice-dark / venice-light / nord-dark alt / venice-rtl Arabic) × 5 viewports (1280×720 / 1440×900 / 1920×1080 / 2560×1440 / mobile-390×844) = 36 captures, 0 failures, plus `EVIDENCE_MANIFEST.json`. Phase 10 — `docs/ROADMAP.md` (this entry), `docs/DOCS_INDEX.md` (registers `docs/design/reference-ui-redesign-evidence/`), and `docs/design/VENICE_FORGE_REFERENCE_UI_REDESIGN_MATRIX.md` (added `tests/accessibility/`, `tests/chat/`, `tests/shell/` ledger sections). **Open work:** Phase 8 dedicated a11y/RTL/responsive pass still requires headed visual + screen-reader acceptance; Phase 9 needs the remaining capture surfaces (Image Studio, Media Studio, Workflows, Documents, Modal/Command Palette, Character Creator, RP Studio, Playground, Privacy, Theme Maker live preview) and the RTL locales other than Arabic shell/chat; Phase 10 must also reconcile the pre-existing duplicate `--color-border-soft`/`--color-border-faint` declarations in `src/styles/theme.css` (lines ~65-66 `rgba()` with allow-marker vs lines ~127-128 `color-mix()`; the later declaration wins). External acceptance (signed/notarized artifacts, two-device sync, qualified native-language review, headed manual a11y) remains tracked under `VF-EXTERNAL-RELEASE-ACCEPTANCE`.

`VF-ATTACHMENT-CONTEXT-2026-09-14 — Model-aware attachment context & guard provenance (partially remediated 2026-09-14; extraction chunking deferred).` Verified against live traffic logs and current `main`: (1) all Venice model records now pass through one canonical normalizer (`normalizeModelInfo` in `src/services/modelClassification.ts`) at every ingestion choke point (`mergeCanonicalModels`/`replaceCanonicalModels` in `modelCatalogCache.ts`, the `useModels` select, `flattenModels`), mapping `model_spec.availableContextTokens`, `context_length`, and camelCase variants into `ModelInfo.contextLength`/`maxOutputTokens` — fixing the silent 8,192-token fallback in the chat budget engine and the picker/context-meter disagreement. (2) Attachment context admission in `use-chat.ts` is no longer governed by the fixed 1 MiB `MAX_TOTAL_CONTEXT_BYTES` ceiling: `computeAttachmentTextAllowance` (`chatContextBudget.ts`) admits extracted attachment text up to the selected model's remaining token budget (context window − output reserve − system prompt − conversation − memory − safety margin), falling back to the legacy byte ceiling only when model context metadata is unknown. (3) The mandatory child-safety guard now splits `<external_attachment>` envelopes (`splitExternalAttachmentSegments` in `childExploitationGuard.ts`): quoted document data remains assessable and still hard-blocks unambiguous CSAM (`csam_request`), but context-dependent youth/adult heuristics no longer treat quoted data as the user's requested intent, and quoted text is excluded from the combined cross-field pass. **Deferred (needs a separate work order):** extraction-stage limits (`MAX_EXTRACTED_TEXT_CHARS = 100_000`, `MAX_CODE_CHARS_PER_FILE = 120_000`) still cap per-file extraction regardless of model — proper chunked extraction with prompt-assembly chunk selection is tracked here; the 2026-09-14 work made admission model-aware but did not make extraction model-scalable.

`VF-TYPOGRAPHY-REVIEW-2026-09-14 — Design-refresh review follow-ups (CLOSED 2026-09-14).` The review of commit `33f82b6` ("Refresh chat UI and typography settings") confirmed four contract defects, all remediated on baseline `bdadccfe`: (1) the font-size setting no longer mutates the root `html` font-size — typography scaling is expressed as `--app-font-scale` applied only to type tokens while rem-derived geometry (`--width-*`, spacing, radii) stays invariant; (2) `--color-accent-soft` is derived from the active accent via `color-mix`; (3) solid `Pill` success/warning/danger use their semantic `*-fg` foregrounds; (4) persisted font IDs are canonicalized via `isFontId()`/`normalizeFontId()` through setter, `migrate`, and `merge`. DOM synchronization is owned solely by the reactive effect in `src/App.tsx`. The remaining P2 items were remediated on 2026-09-14: `FontSettingsPanel` migrated to semantic classes (`vf-h2`/`vf-body`/`vf-meta`/`vf-tag`); chat user-message wrapper migrated to named `--width-chat-bubble` (`max-w-chat-bubble`); message action row migrated to `<Toolbar size="sm" bare>`; image/document attachment action buttons migrated to 28×28 `IconButton sm`; and font default text plus range slider `aria-valuetext` localized across all 12 catalogs. Broader tab-by-tab primitive rollout outside the four migrated surfaces remains tracked under `VF-DESIGN-SYSTEM-2026-09-13`.


`VF-DESIGN-SYSTEM-2026-09-13 — Chat & Design System Refresh (four-tab extension locally complete; broader rollout open).` Per `docs/design/CHAT_DESIGN_SYSTEM_REFRESH_2026-09-13.md`, the original system, chat, and shell refresh remains the accepted baseline. The 2026-09-14 worktree extension on baseline `f74d9c7f` surgically migrated the highest-value remaining duplicate patterns in Media Studio, Prompt Library, Scene Composer, and Workflow Templates to `IconButton`, `Pill`, `Toolbar`, `Card`, and `EmptyState`; replaced touched palette-specific status colors with semantic theme tokens; and fixed keyboard activation/disabled semantics for `IconButton.asPlainButton`. Focused accessibility/integration coverage and the UI/theme/i18n/build gates are recorded in `docs/summary_of_work.md`; headless rendered navigation confirmed named toolbars and localized empty states without clipping at 1440×1000. **Open work:** continue the primitive rollout across other tabs, perform headed screen-reader and multi-theme visual acceptance, and obtain explicit authorization before any commit or push. The completed chat refresh is not to be redone.



`VF-AUD-C6-P1-001 — Fix packaged-CSP smoke probe; restore green hosted CI — CLOSED (hosted-verified 2026-09-13).` The 2026-09-12/13 audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12-c6d9bed/`) confirmed the packaged-launch smoke added in `bb29350e` probes eval-blocking via `page.evaluate` (CDP-exempt from page CSP), failing all three hosted `electron-smoke-*` jobs on every commit (run 34743024816). The probe was replaced with a page-context inline event-handler vector and verified against a locally packaged arm64 build (3/3 smoke tests PASS). Remaining acceptance: push and confirm all three hosted `electron-smoke-*` jobs green on the new SHA — **met: hosted CI run 34756782691 on `067dca58` is 11/11 jobs success (all three `electron-smoke-*` green). Publication required one additional fix: the hosted `contracts`/`coverage` jobs exposed a latent `scenario-store.test.ts` cross-test contamination flake (fake-indexeddb save landing after `reset()` → "expected 2, received 3"); fixed in `067dca58` by draining pending macrotasks between the two resets.** Secondary: `VF-AUD-C6-P3-001` capability-token expired-entry reaping — implemented with threshold sweep + regression tests (`customProtocolAccess.test.ts` 21/21). `VF-AUD-C6-DR-001` atomic-replace consolidation — implemented: all remaining durable main-process writers now use `atomicReplaceFile()`/`atomicReplaceFileSync()` (utility gained fsync option + sync variant); `verify-backup-sync` guard patterns updated to the canonical call.

`VF-EXTERNAL-RELEASE-ACCEPTANCE — External and human release evidence (open).` The 2026-09-10 and 2026-09-11 audit findings are locally remediated; evidence packages remain under `docs/audits/`. The canonical `npm run ci`, strict i18n content validation, production HTTP probe, arm64 packaging, packaged Electron smoke, and artifact verification pass. Remaining acceptance requires: exact-SHA hosted CI/CodeQL after publication; signed/notarized macOS and signed Windows release artifacts; explicitly authorized funded-provider calls; two-device sync/recovery; qualified native-language review; and full headed accessibility review (Rules01 administrator synchronization was completed on 2026-09-11 via `scripts/enforce-github-rules.sh`). Optional video enhancement UI remains scoped out until a dedicated enhancement-model surface is approved.

`VF-GENERATION-CONTRACT-PARITY-2026-09-01 — Generation API follow-up after end-to-end audit (open).` Image Studio, image tools, video/music queueing, TTS, Electron/web transports, durable paid-queue submission, error propagation, and current official wire schemas were reconciled on 2026-09-01. Confirmed local defects (Unicode paid-queue failure, reversible prompt-bearing fingerprints, unconditional variants UI, and lost upstream string details) are implemented and verified in `docs/summary_of_work.md`. Workflow image nodes now resolve live `/models` constraints (item 1). The tracked Swagger snapshot and source manifest were refreshed to official `veniceai/api-docs` `main` `2f0f7e41` / `20260911.010226` (item 2). Item 3 scoped 2026-09-11: quote/queue builders already emit optional `upscale_factor` when a caller supplies `1|2|4`. Enhancement-only fields (`enhancement_model`, `target_fps`, softness/creativity/realism/sharp/compression/noise/halo/grain/recover_detail, `h264_output`, `output_format` mp4|prores, `slowdown_factor`) apply only to provider enhancement/upscale models advertised via GET `/models` `constraints.topaz`. Video Studio has no enhancement/upscale model surface, so those fields stay off the default text/image-to-video queue body. A later work order may add an enhancement path gated on live model constraints. The confirmed `wai-Illustrious` 500 is an external Venice worker failure and must remain classified as external until a current minimal-body replay returns differently.

`VF-THEME-ENGINE-V2-2026-08-31 — Theme Engine V2 family model, YAML pipeline, and persistence (committed 2026-08-31, `26807891`).` Core type/schema/resolver/registry work, built-in family conversion, YAML V2 pipeline (`src/theme/yaml/{parse,validate,normalize,serialize}.ts`), family-centric `ThemeMaker.tsx` editing, Electron main-process V2 persistence, and theme test/verifier updates are committed. Deliverables pass: lint, typecheck, `verify:theme-tokens`, `verify:i18n`, `verify:i18n-hardcoded-regressions`, `test:unit:theme`, ThemeMaker tests, theme service/config handler tests, and full `npm test`. Remaining deferred work: visual review and refinement of generated companion variants in `scripts/generate-theme-families.cjs`; further semantic CSS cleanup of debug-categorical colors and static dark surface scale is optional polish.

`VF-VERTEX-FULL-OAUTH-2026-08-31 — Deferred Google Vertex AI full OAuth/service-account implementation (open; design preserved, public type narrowed 2026-08-31).` VF-AUD-20260831-P2-007 closed the previously selectable but guaranteed-failing `authMode: "full"` branch in the renderer and IPC layer. `GoogleVertexConfig` now narrows to `{ authMode: "express"; apiKey: string }`; the unreachable full-mode rejection in `extractGoogleVertexConfig` and the full-mode UI in `ProvidersPanel.tsx` are removed; the prior "rejects full mode" provider-adapter test is removed. The deferred design specification remains authoritative at `docs/superpowers/specs/2026-08-24-deferred-provider-integration-design.md`; full-mode custody (service-account JSON parsing, OAuth token mint/refresh, project/location validation, main-process token endpoint selection) is intentionally not implemented in this pass and is gated on a separate work order.

`VF-CAPABILITY-PROVENANCE-2026-08-31 — Custom-protocol capability tokens (implemented).` Renderer display URLs are issued via `app:media:issueCapabilityUrl` / `desktopMedia.resolveUrl` / `resolvePlayableMediaUrl`. `venice-media`, `venice-tts`, and `venice-character-cache` handlers verify a short-lived in-memory token bound to `{ objectId, profileId, sessionId }`. Tokens are revoked on profile switch, renderer destroy, and shutdown. Origin/referer checks remain defense-in-depth. Stable persisted URLs stay tokenless.

`VF-FSM-CLASSIFIER-2026-08-31 — Semantic media classifier backend decision recorded; implementation deferred.` VF-AUD-20260831-P2-009 added `getClassifierCapabilities()` to `src/shared/safety/mediaScreener.ts`. The current production behavior is reported as `semanticImageClassifier: "unavailable" | semanticAudioClassifier: "unavailable" | semanticVideoClassifier: "unavailable"` because no ML backend is registered. The structural heuristic (tracking-pixel, MIME mismatch, magic-byte verification) is the only gate. The decision record at `docs/audits/Records/semantic-media-classifier-decision-2026-09-01.md` selects a **local on-device image classifier** as the canonical next backend, preserving the local-first privacy model and avoiding new network boundaries. Provider-side classification is reserved for re-evaluation only if compliance or accuracy requirements override those advantages. Audio and video semantic classification remain deferred until a mature open or first-party option exists. Implementation is still gated on model/runtime selection, bundle-size acceptance, and supply-chain review.

`VF-EXTERNAL-RELEASE-ACCEPTANCE-2026-08-31` — External release acceptance matrix (BETA/INCOMPLETE; cannot be closed from the local tree). Signed/notarized production artifacts, paid provider operations, multi-device sync, headed manual a11y QA, and native-language review are tracked under `VF-VERIFY-005`. Lint/typecheck/tests/coverage/contracts/build, CodeQL, and exact-SHA packaged smoke are now green. External acceptance remains intentionally NOT complete until signed/paid/two-device/headed evidence is produced on a real publication tag. VF-AUD-20260831-P2-004 added `xcrun stapler validate` and `spctl -a` to the macOS release job, `Get-AuthenticodeSignature` to the Windows release job, and a workflow-generated `release-evidence/` artifact (manifest.json, per-platform signature files, checksums.sha256, metadata.json) uploaded as a workflow artifact and attached to the draft release. The portable Windows executable is explicitly handled (it surfaces a warning instead of failing the job when unsigned). VF-AUD-20260831-P2-011 added failure-only smoke diagnostics; the 2026-09-01 repair routes all platforms through a standalone sanitized writer that records repository-relative paths and never loads TypeScript through ordinary Node.

External signed / paid / two-device / screen-reader release evidence remains the only release blocker (currently BETA/INCOMPLETE):

1. **VF-I18N-NATIVE-REVIEW-001** — **OPEN.** All non-English catalogs remain `first-pass-machine` per `docs/i18n/native-review-status.json`. Structural key coverage is complete, but native-speaker/qualified-human review has not been performed and the locales must not be marked `isProductionComplete: true` until `native-review-status.json` records a reviewer and date.

## Active Audit Reconciliation

The most recent local audit lives at `docs/audits/Records/Venice_Forge-audit-evidence-20260717-031029/EVIDENCE_MANIFEST.md`. Its twelve findings (`VF-SCAN-20260717-031029-001..012`) are tracked here; session evidence, regression-guard diffs, and validation results belong in `docs/summary_of_work.md`. Status here reflects only the live tree; audit text is immutable.

- **P3 bounded debt — still open.**
  - `VF-SCAN-20260717-031029-010..012` (documentation trackers): tracked only via `docs/summary_of_work.md` Open TODO Ledger; no code change required.
  - `VF-GOVERNANCE-2026-08-25-002` (Commit Provenance): improve future traceability with meaningful commit subjects and signed commits/tags where practical.

## Audit Input

- The 2026-09-12 current-`main` re-audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12-current-main/`) found 55 items (13 P1). Remediation closed all 13 P1s, implementable P2s, and the confirmed P3s listed in `REMEDIATION_STATUS.md`, including Replicate image dispatch, visual-workflow persist-cap warnings, and packaged CSP smoke auto-run when a binary exists. `VF-CAPABILITY-PROVENANCE-2026-08-31` is implemented. Packaged CSP/onboarding smokes still skip in `npm test` when no packaged executable is present.
- The 2026-09-12 exhaustive audit (`docs/audits/venice-forge-exhaustive-audit-2026-09-12/`) was fully remediated on `main` (all 4 P1, 6 P2, 5 P3, 2 DR, and 3 TG items resolved and verified with regression tests). Detailed validation and execution history are recorded in `docs/summary_of_work.md`.
- The 2026-07-17 03:10 deep scan was fully remediated. The current task listing in this roadmap is the only live truth surface for the audit's findings.
- Prior audit evidence bundles (`Venice_Forge-audit-results-20260716-224749`) live under the `docs/audits/Records/` history directory and are no longer authoritative retained evidence.
- `VF-SCAN-20260716-001..024` were reconciled in the 2026-07-16 current-commit session recorded in `docs/summary_of_work.md`.
- `VF-SCAN-20260717-001..012` from the prior local audit were closed in the 2026-07-17 prior session (`docs/summary_of_work.md`), subject to the bounded debt inventory above. Findings `010` and `011` remain accepted incremental architecture guidance rather than runtime defects.
- `VF-SCAN-20260717-013..016` remain product/release scope rather than hidden local defects. Deferred providers/transports stay fail-closed, orphan deletion stays analysis-only pending quarantine/rollback, and signed/paid/two-device/headed proof remains represented only by `VF-VERIFY-005`.
- `VF-AUDIT-001` was completed through the live GitHub APIs on 2026-07-16: `main` is protected with strict required checks, review/code-owner/last-push approval, admin enforcement and conversation resolution.
- `VF-AUDIT-006` was completed in bounded runtime/toolchain batches (Express 5, dotenv 17, proxy middleware 4, PDF.js 6, Electron 43, ESLint 10, React Hooks ESLint 7, globals 17, Vite 8 and the React Vite plugin 6). `@types/node` intentionally stays on 22 to match the runtime and TypeScript stays on 5.8 because `typescript-eslint` 8.64 declares `<6.1.0`. `npm outdated` showing Node 26 types or TypeScript 7 does not constitute an applicable update for this project.
