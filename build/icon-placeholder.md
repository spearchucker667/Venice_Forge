# Venice Forge Icon Placeholder

`build/icon.ico` is a generated placeholder so local and CI Windows packaging can fail or pass deterministically.

Run:

```bash
npm run generate:icon
```

Replace `build/icon.ico` with final signed-release artwork before public distribution.
